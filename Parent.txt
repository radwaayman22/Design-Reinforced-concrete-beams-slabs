package p;
public class Parent  {
	//Initialize variables
	double fy;
	double fcu;
	double b;
	double d;

	Parent(String x ,String y ,String z , String w ){
		b = Double.parseDouble(x);
		d = Double.parseDouble(y);
		fy = Double.parseDouble(z);
		fcu = Double.parseDouble(w);	
	}
}